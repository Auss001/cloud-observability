---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install baseline system utilities
  apt:
    name:
      - curl
      - wget
      - git
      - ufw
      - htop
    state: present